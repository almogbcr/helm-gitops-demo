replicaCount: 1

image:
  repository: almogbcr/simpleflaskapp
  tag: "2117081"
  pullPolicy: Always  

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

container:
  port: 8000

autoscaling:
  enabled: false

serviceAccount:
  create: false
  name: ""

ingress:
  enabled: true
  host: myapp-dev.local

resources: {}
