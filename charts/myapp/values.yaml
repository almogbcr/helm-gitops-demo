replicaCount: 1

image:
  repository: almogbcr/simpleflaskapp
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

service:
  type: NodePort
  port: 80
  nodePort: 30007

container:
  port: 8000

autoscaling:
  enabled: false

serviceAccount:
  create: false
  name: ""

resources: {}